{
  "name": "landing-page-chrome-extension",
  "version": "1.0.0",
  "description": "Landing page for a Chrome extension",
  "main": "index.js",
  "scripts": {
    "dev": "tailwindcss -i ./input.css -o ./dist/output.css --watch",
    "build": "tailwindcss -i ./input.css -o ./dist/output.css --minify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "chrome-extension",
    "landing-page",
    "tailwindcss"
  ],
  "author": "Your Name",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2",
    "tailwindcss": "^3.3.0"
  }
}
